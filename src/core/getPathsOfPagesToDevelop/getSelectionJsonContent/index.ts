import { getPathsContainNodeConfig } from '../../../shared/index'

export default function ( paths: string[] = [] ): string {
	let json: any = []

	// const
	paths.map( path => {

		let cachedData:any = {}
		const pathsContainNodeConfig: string[] = getPathsContainNodeConfig( path )
		json.push(  )
	} )


	return ''
}

